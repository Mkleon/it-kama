import React from 'react';
import preloader from '../../../assets/images/preloader.gif';

const Preloader = () => {
    return (
        <div>
            <img scr={preloader} alt='preloader' />
        </div>);
};

export default Preloader;